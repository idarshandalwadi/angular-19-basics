import { ProductcodePipe } from './productcode.pipe';

describe('ProductcodePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductcodePipe();
    expect(pipe).toBeTruthy();
  });
});
